#using function

class Solution {
public:
    vector<int> sortArray(vector<int>& nums) {
     sort(nums.begin(),nums.end());
        return nums;
    }
};

#TLE in insertion, bubble, selection sorting,quick sort
